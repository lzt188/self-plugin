# FAQ

- **Manual mode URL rejected?** Must be `http://` or `https://`.
- **Direct mode** only clears DSH's proxy env vars; OS proxy is untouched.
- **Windows** proxy is read from the registry `Internet Settings` (`ProxyServer` / `ProxyOverride`).
